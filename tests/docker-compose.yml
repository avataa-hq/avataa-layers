version: '3.7'
services:
  minio_container:
    image: docker.io/bitnami/minio:2022
    ports:
      - '9000:9000'
    environment:
      - MINIO_ROOT_USER=minio_admin_user
      - MINIO_ROOT_PASSWORD=minio_password
      - MINIO_DEFAULT_BUCKETS=layerbucket
